// Stub - the reference API is not used in this integration (no server persistence in editor).
export const api = {
  get: async () => ({ data: null }),
  post: async () => ({ data: null }),
  put: async () => ({ data: null }),
  delete: async () => ({ data: null }),
  patch: async () => ({ data: null }),
};
